import QtQuick

/// Minimal window skeleton for S1.4a — bootstrap the Qt Quick render loop.
/// S1.4a 最小窗口骨架 — 引导 Qt Quick 渲染循环。
Window {
    visible: true
    title: "IndusScope"
    width: 800
    height: 600
}
